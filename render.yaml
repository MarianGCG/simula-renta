services:
  - type: web
    name: simula-renta
    env: python
    plan: starter
    buildCommand: "./build.sh"
    startCommand: "gunicorn SimulaRenta.wsgi:application --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.5